<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.fragment.app.FragmentContainerView
            android:id="@+id/content"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_above="@id/bottom_bar" />

        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/bottom_bar"
            android:layout_width="match_parent"
            android:layout_height="?actionBarSize"
            android:layout_alignParentBottom="true"
            android:background="@color/white_0_2"
            app:itemHorizontalTranslationEnabled = "false"
            app:itemIconTint="@color/home_screen_bottom_selector"
            app:itemTextAppearanceActive="@style/home_screen_bottom_bar_font"
            app:itemTextAppearanceInactive="@style/home_screen_bottom_bar_font"
            app:itemTextColor="@color/home_screen_bottom_selector"
            app:menu="@menu/home_screen_menu"
            android:visibility="gone"/>
    </RelativeLayout>
</layout>

